<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />

</head>

<body>
    <% if (locals.session && session.user) { %>
        <style>
            body {background-color: <%= session.user.color %>}
          </style>
            <% } %>
    <main>
    <section class="form_section">
        <h1>Práctica Integradora Session y Cookies</h1>
        <form action="/users/form" method="post">
            <div>
                <label for="name">Escriba su nombre</label>
                <input type="text" name="name" id="name">
                <% if (locals.errors && errors.name) { %>
                    <small class="text-danger"><%= errors.name.msg %> </small>
                <% } %>
            </div>
            <div>
                <label for="color">Seleccione un color:</label>
                <select name="color" id="color">
                    <option value="" selected>Seleccionar...</option>
                    <option value="red">ROJO</option>
                    <option value="blue">AZUL</option>
                    <option value="yellow">AMARILLO</option>
                    <option value="pink">ROSA</option>
                    <option value="brown">MARRÓN</option>

                </select>
                <% if (locals.errors && errors.color) { %>
                    <small class="text-danger"><%= errors.color.msg %> </small>
                <% } %>
            </div>
            <div>
                <label for="email">Ingrese su email</label>
                <input type="email" name="email" id="email">
                <% if (locals.errors && errors.email) { %>
                    <small class="text-danger"><%= errors.email.msg %> </small>
                <% } %>
            </div>
            <div>
                <label for="age">Ingrese su edad</label>
                <input type="text" name="age" id="age">
                <% if (locals.errors && errors.age) { %>
                    <small class="text-danger"><%= errors.age.msg %> </small>
                <% } %>
            </div>
            <div>
            <div>
                <label for="recordarColor">Recordar color</label>
                <input type="checkbox" name="recordarColor" id="recordarColor">
            </div>
            <% if (locals.session && session.user) { %>

            <div>
                <a href="/users/forget"><button class="olvidar" type="button">Olvidar el color</button></a>
                <a href="/">Probar si el color se visualiza en segunda vista aquí</a>
            </div>
            <% } %>
            <div>
                <button type="submit">Enviar</button>
            </div>
        </form>
        
        
    </section>
   
         
        <% if (locals.session && session.user) { %>
        <p> Hola <%= session.user.name %>,</p>
        <p> elegiste el color: <% if (session.user.color == "red") { %>
         Rojo
        <% } %> <% if (session.user.color == "brown") { %>
         Marron
        <% } %> <% if (session.user.color == "blue") { %>
            Azul
           <% } %> <% if (session.user.color == "yellow") { %>
            Amarillo
           <% } %> <% if (session.user.color == "pink") { %>
            Rosa
           <% } %>
    </p>
        <p>tu email es: <%= session.user.email  %> y</p>
        <p>tu edad <%= session.user.age == "" ? "no fue declarada" : "es: " + session.user.age + " años" %></p>

<%}%>

</main>
    
</body>
</html>